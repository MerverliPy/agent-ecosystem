{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main DeskAgent window: core commands only (local-first, no remote calls).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
